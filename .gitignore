# secrets / env
.env

# python
__pycache__/
*.pyc
*.pyo

# airflow
logs/

# telegram session (генерируется локально, на сервер копируется вручную)
telegram_session/
*.session
*.session-journal

# xray-прокси (личная VPN-подписка, содержит секреты — не коммитим)
xray/config.json

# обученные модели (лежат в volume, не в git)
model_store/*.joblib
model_store/*.pkl

# прочее
.DS_Store
__MACOSX/
